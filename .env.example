# Local development only (running outside Docker).
# For the Docker deploy, see deploy/*.env.example instead.

MULCHD_DATA_PATH=.mulch
MULCHD_DB_URL=sqlite://mulchd.db
MULCHD_HOST=127.0.0.1
MULCHD_PORT=8000
# python -c "import secrets; print(secrets.token_hex(32))"
MULCHD_SECRET_KEY=
MULCHD_ADMIN_PASSWORD=
